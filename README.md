# DevTools Network Response Inspector

A Chrome DevTools extension that captures network responses and allows searching inside API responses.

## Features
- Capture network responses
- Search inside responses

## Usage
1. Open Chrome DevTools
2. Navigate to the extension panel
3. Browse a website
4. Use the search bar to find values inside responses
